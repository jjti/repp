package assemble

import "github.com/jjtimmons/decvec/internal/dvec"

// traverse the slice of assemblies and convert them into Fragments that
// could be pieced together using Gibson Assembly
func traverse(assemblies []assembly) (fragments [][]dvec.Fragment) {

	return
}
